meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  install:
    - pip install -r requirements.txt

run:
  scriptName: "uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level info"
