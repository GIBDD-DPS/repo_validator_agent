@'
# Repo Validator Agent

AI-powered repository analyzer and fixer.

## Структура проекта
- `main.py` — FastAPI приложение (точка входа)
- `core/` — модули анализа и фиксов
- `configs/` — конфигурации
- `fixed_files/` — результаты и автопоправки
- `prizolov_integration/` — интеграция с внешними системами
- `interfaces/` — CLI и интерфейсы
- `tests/` — тесты

## Быстрый старт локально
1. Создать виртуальное окружение:
   ```bash
   python -m venv venv
   # Windows PowerShell
   venv\Scripts\Activate.ps1
